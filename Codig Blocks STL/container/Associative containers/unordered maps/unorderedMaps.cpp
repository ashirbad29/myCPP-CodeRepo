#include <iostream>
#include <algorithm>
#include <vector>
#include <unordered_map>

using namespace std;

int main() {
	// unordered_maps implements hashtables underneath
	// insertion deletion and search works in o(1) on average
	// this is the best thing about unordered maps

	 // ordering of keys are not sorted here

	return 0;
}